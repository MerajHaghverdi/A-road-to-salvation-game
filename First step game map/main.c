#include <stdio.h>
#include <windows.h>
#include <conio.h>
#include <time.h>
#include <ctime>

int rows,columns;
int main()
{
    printf("enter rows = ");
    scanf("%d",&rows);
    printf("enter columns = ");
    scanf("%d",&columns);
    char map[rows][columns];
    return 0;
}
